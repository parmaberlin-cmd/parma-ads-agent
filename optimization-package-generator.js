function buildOptimizationPackage(experiment={},evidence={}){const required=['id','mechanism','rollback','permission_class'];const missing=required.filter(k=>!experiment[k]);const evidence_ready=evidence.fresh===true&&evidence.measurement_verified===true&&evidence.mature===true;return {valid:missing.length===0,missing,experiment_id:experiment.id||null,mechanism:experiment.mechanism||null,rollback:experiment.rollback||null,permission_class:experiment.permission_class||null,evidence_ready,proposal_ready:missing.length===0&&evidence_ready,simulation_only:true,execution_allowed:false,spend_authorized:false,external_write_authorized:false};}
module.exports={buildOptimizationPackage};
