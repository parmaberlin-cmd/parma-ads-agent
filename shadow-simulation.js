function simulateShadowDays({days=[],buildReport}={}){if(typeof buildReport!=="function")throw new Error("buildReport is required");return days.map((input,index)=>{const report=buildReport(input);return {day:index+1,now:input.now||null,writes_allowed:report.writes_allowed,conversion_status:report.conversion_integrity?.status||"unknown",funnel_status:report.funnel?.status||"unknown",estimated_waste_eur:Number(report.waste?.estimated_waste_eur||0),opportunity_count:(report.opportunities||[]).length,primary_priorities:(report.daily_manager?.primary_priorities||[]).map(x=>x.code),automation_allowed:report.safety_gate?.automation_allowed===true};});}
function summarizeSimulation(rows=[]){return {days:rows.length,all_read_only:rows.every(r=>r.writes_allowed===false),autonomous_write_days:rows.filter(r=>r.automation_allowed).length,max_estimated_waste_eur:rows.reduce((m,r)=>Math.max(m,Number(r.estimated_waste_eur||0)),0),days_with_priorities:rows.filter(r=>r.primary_priorities.length>0).length,conversion_statuses:[...new Set(rows.map(r=>r.conversion_status))],funnel_statuses:[...new Set(rows.map(r=>r.funnel_status))]};}
module.exports={simulateShadowDays,summarizeSimulation};
