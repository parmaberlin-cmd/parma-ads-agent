function assessMobileContinuity(input={}){const checks={intent_known:Boolean(input.intent),ad_message_present:Boolean(input.ad_message_present),landing_verified:Boolean(input.landing_verified),primary_cta_verified:Boolean(input.primary_cta_verified),destination_verified:Boolean(input.destination_verified),mobile_verified:Boolean(input.mobile_verified)};const known=Object.values(checks).filter(Boolean).length;const score=Math.round(known/Object.keys(checks).length*100);const complete=Object.values(checks).every(Boolean);return {checks,score,status:complete?'structurally_complete':'incomplete_or_unknown',outcome_verified:false,site_change_supported:false,execution_allowed:false,rule:'Landing continuity is structural evidence, not proof of incremental customers.'};}
module.exports={assessMobileContinuity};
