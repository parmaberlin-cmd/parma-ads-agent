# Parma Ads Agent — checkpoint 2026-08-26

## Stato verificato

- Google Ads, Meta Ads e GA4 risultano raggiungibili dai collector live in sola lettura.
- Google Ads: 2 campagne idonee; ultimi 7 giorni: EUR 48,44 di spesa, 3.027 impression, 84 clic, 5 conversioni.
- GA4: 174 `booking_completed` negli ultimi 7 giorni, di cui 13 attribuiti a Google paid.
- Meta Ads: 9 campagne censite, nessuna attiva, 5 con diagnostiche da classificare; nessuna spesa osservata.
- Shadow history: 56 esecuzioni, archivio Railway duraturo e sano, zero violazioni di sicurezza.
- Scritture, attivazioni e spesa restano disabilitate.

## Correzioni locali completate

1. Ripristinato il workflow `meta-draft-inventory` come esclusivamente manuale.
2. Resa riproducibile l'installazione CI con `npm ci` e aggiunto il lockfile.
3. Aggiunto `.gitignore` per escludere dipendenze e file `.env`.
4. Rafforzato il confronto della chiave API con digest e confronto a tempo costante.
5. Corretto il falso allarme `LANDING_UNAVAILABLE`: l'assenza di un evento GA4 ora indica stato sconosciuto, non indisponibilità del sito.
6. Allineato lo stato dello storage storico tra summary, dashboard e ciclo operativo.
7. Separata la freschezza tecnica dei collector dalla data dell'ultimo evento commerciale, eliminando il falso allarme GA4 “stale” quando non ci sono nuove prenotazioni paid.
8. Preparato un validatore Google definitivo, esclusivamente GET, per la campagna concordata; restituisce solo metriche aggregate e controlli di coerenza.
9. Migliorata la diagnostica Meta: conteggi per singolo problema e codici sconosciuti sicuri, senza nomi o ID degli oggetti pubblicitari.
10. Corretti i workflow CI affinché usino tutti il lockfile senza eseguire lifecycle script.

## Doppia validazione

### Livello 1 — locale

- Controllo sintattico: superato.
- Test automatici: 379/379 superati.
- Audit dipendenze di produzione: 0 vulnerabilità note.
- Scansione credenziali: nessuna credenziale reale trovata nei file tracciati; i match rilevati sono fixture di test intenzionali.

### Livello 2 — runtime remoto, sola lettura

- Endpoint pubblici principali: HTTP 200.
- Payload pubblici: conformi al filtro anti-segreti.
- Endpoint protetti senza chiave: HTTP 401.
- Ultimo workflow GitHub `google-live-access`: superato.
- Ultimo workflow GitHub `shadow-daily`: superato.

## Restano da fare con autorizzazione o accesso privato

1. Eseguire il test protetto della campagna Google concordata e confrontare i risultati con l'interfaccia Google Ads.
2. Ruotare `PARMA_AGENT_API_KEY`, perché una versione precedente era stata esposta; la rotazione richiede modifica Railway e successivo redeploy.
3. Pubblicare le correzioni locali su GitHub e verificare i workflow CI/Railway.
4. Correggere il tracking GA4 di `reservation_page_view` e `reservation_start`; oggi risultano configurati ma mai osservati.
5. Eseguire la nuova diagnostica Meta dopo il deploy e preparare un solo draft PAUSED; nessuna creazione o attivazione senza approvazione esplicita.

## Checkpoint Git locale

- `878c697` — hardening runtime, CI e sicurezza.
- `0cfd83d` — test Google definitivo e correzione freschezza telemetry.
- `960e490` — diagnostica Meta aggregata e sanitizzata.
- Nessun commit è stato pubblicato e nessun deploy è stato eseguito.

## Ordine consigliato alla ripresa

1. Revisione e pubblicazione delle correzioni locali.
2. Rotazione della chiave API Railway.
3. Test Google protetto sulla campagna concordata.
4. Riparazione tracking GA4 e nuova osservazione shadow.
5. Diagnosi Meta e proposta PAUSED pronta per approvazione.
