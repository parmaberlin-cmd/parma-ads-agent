# Parma Ads Agent — Master Roadmap

## Current operating state

DNA v1.2 is ACTIVE. Google Ads authenticated production reads and GA4 production read-only access are verified. The current P0 is conversion integrity: reconcile Google Ads, GA4 and eventually Wix business ground truth without weakening safety gates.

Progress percentages are intentionally not increased for branch-only code or documentation. Capability promotion requires verified end-to-end DONE criteria.

## Milestones

### M0 — DNA v1.2 ACTIVE
DONE. Genome materialization, Bootstrap and Recovery are verified and durable.

### M1 — Google Ads end-to-end
DONE for authenticated read-only access and live campaign intelligence. Exact-date reader improvements remain behind publication gate until merged/deployed.

### M2 — Google intelligence
IN PROGRESS. Search terms, keyword overlap, RSA structure, rank/budget, device, hour and geo diagnostics are available in read-only analysis. Conversion-sensitive optimization remains fail-closed.

### M3 — Google + GA4 decision engine
IN PROGRESS. Measurement Contract, conversion-confidence model, exact-date GA4 semantics reader and cross-platform reconciliation matrix exist. Completion requires timezone/date-basis/counting/attribution alignment plus trustworthy business ground truth.

### M4 — Meta + Google integrated control
PARTIAL. Meta issue classification and account-level-pattern candidate detection exist, but external account/security/payment evidence may still be required.

### M5 — Persistent autonomous operator
PARTIAL. Durable state, shadow history, policy gates, blocker classification and autonomous backlog exist. Completion requires verified outcome learning and bounded execution within delegation.

## Access-independent workstream

The Agent should autonomously continue any task that does not require external identity/account UI access, campaign/tracking/spend mutation, merge/deploy approval or secrets. The authoritative queue is `state/AUTONOMOUS_BACKLOG.json`.

Completed in the current diagnostic phase:
- stacked branch synchronization with exact-date base;
- production-linked exact-date parser test;
- Measurement Contract;
- conversion reconciliation and confidence guards;
- live-payload no-write Google analysis;
- local/walk-in search-intent protection;
- keyword-overlap diagnostics;
- RSA structural diagnosis independent of untrusted conversions;
- rank/budget, device, schedule and geo descriptive diagnostics;
- no-write Google optimization simulation;
- conservative Meta restriction-pattern diagnosis;
- reversible mutation-package schema;
- blocker-type separation between software, data/maturity, external access and permission gates.

## External checkpoints

These are not software blockers:
- correct Wix account/site access for reservation ground truth;
- Meta UI/account evidence if the read-only repo evidence cannot prove the restriction cause.

## Permission checkpoints

These remain independent even if diagnostics become high-confidence:
- merge/deploy of new readers;
- campaign, tracking, keyword, ad, targeting or budget mutation;
- spend activation or payment/security changes.

## Progress rule

No progress increase from repeated debugging attempts, documentation alone or undeployed code. Each capability promotion must cite DONE criteria and end-to-end evidence.

## Next sequence after external access becomes convenient

1. Read Wix reservation ground truth for the exact reconciliation window.
2. Run Google conversion-date/action metadata and GA4 exact-date semantics readers after an authorized publication gate.
3. Recompute conversion confidence and Measurement Contract status.
4. Convert only evidence-supported optimization simulations into approval-ready reversible mutation packages.
5. Execute only within explicit delegation, then measure outcomes and persist learning.
