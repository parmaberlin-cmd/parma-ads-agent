const test=require('node:test');const assert=require('node:assert/strict');const {buildOperationalDashboard}=require('../operational-dashboard');
test('dashboard is compact sanitized and permanently non-writing',()=>{const dashboard=buildOperationalDashboard({summary:{generated_at:'2026-08-23T12:00:00Z',source_health:{google:false,ga4:true,meta:true},data_quality:{confidence:'partial',blockers:['google_unavailable']},tracking:{reservation_page_view:{configured:true,observed:true},reservation_start:{configured:true,observed:false},booking_completed:{configured:true,observed:true}},conversion_integrity:{status:'healthy'},history:{total_runs:5},primary_priorities:[{code:'FIX_GOOGLE',severity:'high',source:'google',requires_authorization:false}]},cycle:{blocked_stages:['validate'],operational:{alerts:[{severity:'high',code:'google_unavailable',source:'data_quality'}]}},promotion:{promotion_ready:false,autonomy_class:'observe_and_propose',readiness_score:70,blockers:['google_live_validation']}});assert.equal(dashboard.sources.google,'blocked');assert.equal(dashboard.sources.ga4,'ok');assert.deepEqual(dashboard.tracking.reservation_start,{configured:true,observed:false});assert.equal(dashboard.writes_allowed,false);assert.equal(dashboard.execution_allowed,false);assert.equal(dashboard.spend_allowed,false);assert.ok(dashboard.blockers.includes('google_unavailable'));assert.ok(dashboard.blockers.includes('cycle_validate'));});
test('dashboard truncates unbounded alert and blocker lists',()=>{const dashboard=buildOperationalDashboard({summary:{data_quality:{blockers:Array.from({length:30},(_,i)=>`b${i}`)}},cycle:{blocked_stages:[],operational:{alerts:Array.from({length:30},(_,i)=>({severity:'low',code:`a${i}`,source:'x'}))}},promotion:{blockers:[]}});assert.equal(dashboard.blockers.length,12);assert.equal(dashboard.alerts.length,8);});