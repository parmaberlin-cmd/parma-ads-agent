const test=require('node:test');const assert=require('node:assert/strict');const {scanPublicPayload,assertPublicPayloadSafe}=require('../public-output-safety');
test('safe health payload passes',()=>{const payload={success:true,source_health:{google:false,ga4:true,meta:true},diagnostic:{category:'developer_token',reason:'basic_access_required'},writes_allowed:false};assert.deepEqual(scanPublicPayload(payload),[]);assert.equal(assertPublicPayloadSafe(payload),payload);});
test('credential-like keys are rejected recursively',()=>{for(const key of ['access_token','refresh_token','developer_token','client_secret','api_key','authorization']){const payload={nested:{[key]:'secret'}};assert.throws(()=>assertPublicPayloadSafe(payload),/public payload safety contract violated/);}});
test('advertising object id keys are rejected from public payloads',()=>{for(const key of ['campaign_id','adset_id','ad_id','creative_id','ad_account_id','customer_id'])assert.throws(()=>assertPublicPayloadSafe({[key]:'123'}));});
test('Bearer credential values are rejected even under an innocent key',()=>{assert.throws(()=>assertPublicPayloadSafe({message:'Bearer abcdefghijklmnopqrstuvwxyz'}),/public payload safety contract violated/);});