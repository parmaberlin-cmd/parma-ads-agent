const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
test('full live shadow snapshot evaluates and exposes fail-closed data quality',()=>{const source=fs.readFileSync(path.join(__dirname,'..','full-live-shadow-data.js'),'utf8');assert.ok(source.includes("require('./shadow-data-quality')"));assert.ok(source.includes('evaluateShadowDataQuality(snapshot,{now})'));assert.ok(source.includes('assertQualityFailClosed(data_quality)'));assert.ok(source.includes('data_quality'));});
test('collectors stamp collection time independently of business events',()=>{const live=fs.readFileSync(path.join(__dirname,'..','live-shadow-data.js'),'utf8');const ga4=fs.readFileSync(path.join(__dirname,'..','ga4-shadow-data.js'),'utf8');assert.ok(live.includes('collected_at'));assert.ok(ga4.includes('collected_at'));});