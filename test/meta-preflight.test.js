const test=require('node:test');const assert=require('node:assert/strict');const {runMetaPausedPreflight}=require('../meta-preflight');
function draft(){return {campaign:{name:'test',objective:'OUTCOME_TRAFFIC',status:'PAUSED',special_ad_categories:[]},adSet:{name:'set',daily_budget:600,billing_event:'IMPRESSIONS',optimization_goal:'LINK_CLICKS',bid_strategy:'LOWEST_COST_WITHOUT_CAP',targeting:{publisher_platforms:['instagram']},status:'PAUSED'},creative:{name:'creative',object_id:'page',instagram_user_id:'ig',source_instagram_media_id:'media'},ad:{name:'ad',status:'PAUSED'},policy:{may_activate:false}}}
test('preflight is green only with both readiness levels green',()=>{const r=runMetaPausedPreflight({draft:draft(),assets:{page_id:'page',instagram_user_id:'ig',source_instagram_media_id:'media'},writeGateEnabled:true,approvalTokenOk:true});assert.equal(r.ready,true);assert.equal(r.maximum_attempts,1);assert.equal(r.may_activate,false)});
test('preflight fails closed without write gate',()=>{const r=runMetaPausedPreflight({draft:draft(),assets:{page_id:'page',instagram_user_id:'ig',source_instagram_media_id:'media'},approvalTokenOk:true});assert.equal(r.ready,false);assert.ok(r.level_1_readiness.blockers.includes('write_gate_enabled'))});
test('conservative payload never contains ACTIVE',()=>{const r=runMetaPausedPreflight({draft:draft(),assets:{page_id:'page',instagram_user_id:'ig',source_instagram_media_id:'media'},writeGateEnabled:true,approvalTokenOk:true});assert.equal(r.level_2_payload.checks.no_active_literal,true)});
