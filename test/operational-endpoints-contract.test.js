const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');const bootstrap=fs.readFileSync(path.join(root,'bootstrap.js'),'utf8');const openapi=fs.readFileSync(path.join(root,'openapi.yaml'),'utf8');
test('sanitized dashboard and cycle endpoints are implemented and documented',()=>{assert.ok(bootstrap.includes('app.get("/health/agent-dashboard"'));assert.ok(bootstrap.includes('app.get("/health/agent-cycle"'));assert.ok(openapi.includes('/health/agent-dashboard:'));assert.ok(openapi.includes('operationId: getSanitizedAgentDashboard'));assert.ok(openapi.includes('/health/agent-cycle:'));assert.ok(openapi.includes('operationId: getSanitizedReadonlyCycle'));});
test('dashboard and cycle paths remain zero-write',()=>{assert.ok(bootstrap.includes('buildOperationalDashboard'));assert.ok(bootstrap.includes('assertReadonlyCycleSafe'));assert.ok(bootstrap.includes('writes_allowed: false'));});