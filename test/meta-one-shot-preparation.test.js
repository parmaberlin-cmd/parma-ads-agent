const test=require('node:test');const assert=require('node:assert/strict');const {prepareMetaPausedOneShot}=require('../meta-one-shot-preparation');
function base(knownPartial={}){return {draft:{campaign:{name:'c',objective:'OUTCOME_TRAFFIC',status:'PAUSED',special_ad_categories:[]},adSet:{name:'s',lifetime_budget:8400,billing_event:'IMPRESSIONS',optimization_goal:'LINK_CLICKS',bid_strategy:'LOWEST_COST_WITHOUT_CAP',dsa_beneficiary:'Parma',dsa_payor:'Parma',targeting:{publisher_platforms:['instagram']},status:'PAUSED'},creative:{name:'x',object_id:'1',instagram_user_id:'2',source_instagram_media_id:'3'},ad:{name:'a',status:'PAUSED'},policy:{may_activate:false}},assets:{page_id:'1',instagram_user_id:'2',source_instagram_media_id:'3'},writeGateEnabled:true,approvalTokenOk:true,knownPartial};}
test('fresh one-shot preparation can be ready without any write',async()=>{const r=await prepareMetaPausedOneShot({...base(),transport:{get:async()=>{throw new Error('should not read')}}});assert.equal(r.ready,true);assert.equal(r.may_activate,false);assert.equal(r.may_create_duplicate,false)});
test('valid partial chain stays ready and is reused',async()=>{const map={'11':{id:'11',status:'PAUSED'},'22':{id:'22',status:'PAUSED',campaign_id:'11'}};const r=await prepareMetaPausedOneShot({...base({campaign_id:'11',adset_id:'22'}),transport:{get:async p=>map[p.slice(1)]}});assert.equal(r.ready,true);assert.deepEqual(r.execution_order.slice(0,2),['reuse_campaign','reuse_adset'])});
test('invalid partial relationship blocks the real test',async()=>{const map={'11':{id:'11',status:'PAUSED'},'22':{id:'22',status:'PAUSED',campaign_id:'999'}};const r=await prepareMetaPausedOneShot({...base({campaign_id:'11',adset_id:'22'}),transport:{get:async p=>map[p.slice(1)]}});assert.equal(r.ready,false);assert.ok(r.blockers.includes('adset_campaign_mismatch'))});
