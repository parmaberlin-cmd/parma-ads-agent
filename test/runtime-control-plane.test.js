const test=require('node:test');const assert=require('node:assert/strict');const p=require('../runtime-control-plane');
test('read action requires dedicated key',()=>{assert.equal(p.authorizeControlPlane({providedKey:'x',configuredKey:'x',action:'google_access'}).allowed,true);assert.equal(p.authorizeControlPlane({providedKey:'bad',configuredKey:'x',action:'google_access'}).allowed,false)});
test('unknown action fails closed',()=>{assert.equal(p.authorizeControlPlane({providedKey:'x',configuredKey:'x',action:'env_dump'}).allowed,false)});
test('paused write requires gate and explicit approval',()=>{assert.equal(p.authorizeControlPlane({providedKey:'x',configuredKey:'x',action:'meta_paused_draft',writeGateEnabled:false,approvalOk:true}).allowed,false);assert.equal(p.authorizeControlPlane({providedKey:'x',configuredKey:'x',action:'meta_paused_draft',writeGateEnabled:true,approvalOk:false}).allowed,false);assert.equal(p.authorizeControlPlane({providedKey:'x',configuredKey:'x',action:'meta_paused_draft',writeGateEnabled:true,approvalOk:true}).allowed,true)});
test('capabilities never expose secrets or Railway mutation',()=>{const c=p.publicRuntimeCapabilities();assert.equal(c.secrets_exposed,false);assert.equal(c.environment_mutation,false);assert.equal(c.deploy_mutation,false);assert.equal(c.active_ads_write,false)});
