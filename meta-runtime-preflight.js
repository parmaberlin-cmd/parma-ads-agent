const axios = require('axios');
const { APPROVAL_TOKEN, buildPausedReservationDraft, discoverInstagramReelAssets, META_API_VERSION } = require('./meta-paused-draft-next');
const { runMetaRealPreflight } = require('./meta-real-preflight');
const { decideNextMetaStep } = require('./meta-preflight-decision');

const DEFAULT_REEL='https://www.instagram.com/reel/C9M7_b6MayR/';
const DEFAULT_USERNAME='parma.divinibenedetti';
const DEFAULT_LATITUDE=52.499492;
const DEFAULT_LONGITUDE=13.4399793;

function normalizeAccountId(value){const text=String(value||'').trim();if(/^act_\d{1,30}$/.test(text))return text;if(/^\d{1,30}$/.test(text))return `act_${text}`;return null;}
function parseFutureStart(value,now=Date.now()){const date=new Date(String(value||''));if(Number.isNaN(date.getTime())||date.getTime()<=now+15*60*1000)return null;return date.toISOString();}
function cleanError(error){const graph=error?.response?.data?.error||{};return {message:String(graph.message||error?.message||'meta_preflight_failed').replace(/\b\d{8,}\b/g,'[REDACTED_ID]').slice(0,180),type:graph.type||null,code:graph.code||null,subcode:graph.error_subcode||null};}
function normalizeApiVersion(value){const candidate=String(value||META_API_VERSION).trim();return /^v\d+\.0$/.test(candidate)?candidate:META_API_VERSION;}
function runtimeConfig(env=process.env){return {accessToken:env.META_ACCESS_TOKEN||null,adAccountId:normalizeAccountId(env.META_AD_ACCOUNT_ID),apiVersion:normalizeApiVersion(env.META_API_VERSION),dsaBeneficiary:env.META_AD_DSA_BENEFICIARY||null,dsaPayor:env.META_AD_DSA_PAYOR||null,writeGateEnabled:env.META_PAUSED_DRAFT_WRITES_ENABLED==='true',expectedTimezone:env.META_AD_TIMEZONE||'Europe/Berlin',expectedCurrency:env.META_AD_CURRENCY||'EUR'};}
function createReadTransport({accessToken,apiVersion=META_API_VERSION,client=axios}){const http=client.create?client.create({baseURL:`https://graph.facebook.com/${normalizeApiVersion(apiVersion)}`,timeout:20000}):client;return {async get(endpoint,params={}){const response=await http.get(endpoint,{params:{...params,access_token:accessToken}});return response.data;}};}
async function inspectAccountContext(transport,config){const account=await transport.get(`/${config.adAccountId}`,{fields:'account_status,currency,timezone_name'});const timezone=String(account?.timezone_name||'');const currency=String(account?.currency||'').toUpperCase();const blockers=[];if(!timezone)blockers.push('account_timezone_unknown');else if(timezone!==config.expectedTimezone)blockers.push('account_timezone_mismatch');if(!currency)blockers.push('account_currency_unknown');else if(currency!==config.expectedCurrency)blockers.push('account_currency_mismatch');return {readable:true,timezone_name:timezone,expected_timezone:config.expectedTimezone,currency,expected_currency:config.expectedCurrency,timezone_match:timezone===config.expectedTimezone,currency_match:currency===config.expectedCurrency,account_status_present:account?.account_status!=null,blockers};}
function finalizeResult(result){return {...result,decision:decideNextMetaStep(result),write_operation_performed:false,may_activate:false,may_spend:false};}
async function executeRuntimeMetaPreflight({env=process.env,startsAt,httpClient=axios}={}){const config=runtimeConfig(env);const missing=[];if(!config.accessToken)missing.push('META_ACCESS_TOKEN');if(!config.adAccountId)missing.push('META_AD_ACCOUNT_ID');if(!config.dsaBeneficiary)missing.push('META_AD_DSA_BENEFICIARY');if(!config.dsaPayor)missing.push('META_AD_DSA_PAYOR');if(missing.length)return finalizeResult({success:false,mode:'read_only',ready:false,blockers:['configuration_incomplete'],missing_variables:missing});const start=parseFutureStart(startsAt);if(!start)return finalizeResult({success:false,mode:'read_only',ready:false,blockers:['invalid_start_time']});const transport=createReadTransport({accessToken:config.accessToken,apiVersion:config.apiVersion,client:httpClient});const account=await inspectAccountContext(transport,config);const assets=await discoverInstagramReelAssets({transport,adAccountId:config.adAccountId,instagramUsername:DEFAULT_USERNAME,reelPermalink:DEFAULT_REEL});const draft=buildPausedReservationDraft({pageId:assets.page_id,instagramUserId:assets.instagram_user_id,sourceInstagramMediaId:assets.source_instagram_media_id,latitude:DEFAULT_LATITUDE,longitude:DEFAULT_LONGITUDE,dailyBudgetEur:6,durationDays:14,startsAt:start,dsaBeneficiary:config.dsaBeneficiary,dsaPayor:config.dsaPayor});const preflight=await runMetaRealPreflight({transport,adAccountId:config.adAccountId,draft,assets,writeGateEnabled:config.writeGateEnabled,approvalTokenOk:Boolean(APPROVAL_TOKEN)});const blockers=[...account.blockers,...(preflight.blockers||[])];return finalizeResult({...preflight,ready:preflight.ready&&account.blockers.length===0,account,api_version:config.apiVersion,blockers:[...new Set(blockers)]});}
function registerMetaRealPreflightRoute(app,{authorized,env=process.env,httpClient=axios}={}){app.get('/tools/meta/reservation-draft/real-preflight',async(req,res)=>{res.setHeader('Cache-Control','no-store');res.setHeader('Pragma','no-cache');if(typeof authorized==='function'&&!authorized(req))return res.status(401).json({success:false,error:'Unauthorized'});try{const result=await executeRuntimeMetaPreflight({env,startsAt:req.query?.starts_at,httpClient});return res.status(result.ready?200:409).json({...result,endpoint:'meta_real_preflight',write_operation_performed:false,activates_spend:false});}catch(error){return res.status(500).json({success:false,mode:'read_only',ready:false,error:cleanError(error),decision:{state:'blocked',next_action:'resolve_runtime_error',write_allowed:false},write_operation_performed:false,may_activate:false,may_spend:false,activates_spend:false});}});}
module.exports={normalizeAccountId,parseFutureStart,cleanError,normalizeApiVersion,runtimeConfig,createReadTransport,inspectAccountContext,finalizeResult,executeRuntimeMetaPreflight,registerMetaRealPreflightRoute};
