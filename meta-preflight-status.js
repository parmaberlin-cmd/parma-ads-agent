const { executeRuntimeMetaPreflight } = require('./meta-runtime-preflight');

const state = { status:'pending', started_at:null, finished_at:null, result:null, error:null };
function futureStart(){ return new Date(Date.now()+24*60*60*1000).toISOString(); }
function sanitize(result){
 if(!result)return null;
 return {ready:Boolean(result.ready),mode:'read_only',levels:result.levels||null,chain:result.chain||null,blockers:Array.isArray(result.blockers)?result.blockers:[],maximum_attempts:result.maximum_attempts??1,may_activate:false,may_spend:false,account:result.account?{readable:Boolean(result.account.readable),timezone_name:result.account.timezone_name||null,expected_timezone:result.account.expected_timezone||null,currency:result.account.currency||null,expected_currency:result.account.expected_currency||null,timezone_match:Boolean(result.account.timezone_match),currency_match:Boolean(result.account.currency_match),account_status_present:Boolean(result.account.account_status_present),blockers:result.account.blockers||[]}:null};
}
async function run(){if(state.status==='running')return;state.status='running';state.started_at=new Date().toISOString();state.error=null;try{const result=await executeRuntimeMetaPreflight({startsAt:futureStart()});state.result=sanitize(result);state.status='completed';state.finished_at=new Date().toISOString();console.log(JSON.stringify({event:'meta_runtime_preflight',success:true,...state.result}));}catch(error){state.status='failed';state.finished_at=new Date().toISOString();state.error=String(error?.message||error).replace(/\b\d{8,}\b/g,'[REDACTED_ID]').slice(0,180);console.error(JSON.stringify({event:'meta_runtime_preflight',success:false,error:state.error,mode:'read_only',may_activate:false,may_spend:false}));}}
function register(app){app.get('/health/meta-real-preflight-summary',(req,res)=>{res.setHeader('Cache-Control','no-store');if(state.status==='pending'||state.status==='running')return res.status(202).json({success:true,status:state.status,mode:'read_only',may_activate:false,may_spend:false,started_at:state.started_at});if(state.status==='failed')return res.status(500).json({success:false,status:'failed',mode:'read_only',may_activate:false,may_spend:false,error:state.error,finished_at:state.finished_at});return res.json({success:true,status:'completed',finished_at:state.finished_at,...state.result});});}
module.exports={state,run,register,sanitize,futureStart};
